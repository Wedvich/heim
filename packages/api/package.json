{
  "name": "@heim/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "node --watch src/index.ts",
    "lint": "oxlint .",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@heim/domain": "workspace:*",
    "express": "^5.1.0"
  },
  "devDependencies": {
    "@heim/infra": "workspace:*",
    "@types/express": "^5.0.0"
  }
}
