{
  "name": "@heim/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "true",
    "dev": "true",
    "lint": "oxlint .",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsgo --noEmit"
  },
  "dependencies": {
    "@heim/domain": "workspace:*"
  },
  "devDependencies": {
    "@heim/infra": "workspace:*"
  }
}
