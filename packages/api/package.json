{
  "name": "@heim/api",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "true",
    "dev": "true",
    "lint": "true",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "true"
  },
  "dependencies": {
    "@heim/domain": "workspace:*"
  }
}
