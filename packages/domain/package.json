{
  "name": "@heim/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "true",
    "dev": "true",
    "lint": "true",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "true"
  }
}
