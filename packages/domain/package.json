{
  "name": "@heim/domain",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "oxlint .",
    "test": "vitest run --coverage",
    "test:watch": "vitest",
    "typecheck": "tsgo --noEmit"
  }
}
